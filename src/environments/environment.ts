// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
  apiKey: "AIzaSyC6q0gYwxXUbsyn-GCdpeMQs_adJ829wa0",
  authDomain: "formbuilder-be8d1.firebaseapp.com",
  projectId: "formbuilder-be8d1",
  storageBucket: "formbuilder-be8d1.appspot.com",
  messagingSenderId: "933554371771",
  appId: "1:933554371771:web:a8d73f024fe5f6f53abd9e",
  measurementId: "G-9W0BLCT58B"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
